services:
  localstack:
    image: localstack/localstack:3
    ports:
      - "4566:4566"
    environment:
      - SERVICES=lambda,apigateway,dynamodb,s3,cloudformation,iam,sts,ssm,logs
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"